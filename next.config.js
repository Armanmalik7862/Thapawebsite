/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    images: {
      domains: ['occ-0-3933-116.1.nflxso.net', 'via.placeholder.com'], // Add the relevant domain(s) for your images
    },
  };

 